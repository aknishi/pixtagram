<!DOCTYPE html>
<html>
  <head>
    <title>Pixtagram</title>
    <link href="https://fonts.googleapis.com/css?family=Abel" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Abel|Questrial" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <script type="text/javascript">
      window.cameraURL = "<%= image_url('camera-logo.png') %>"
      window.heartURL = "<%= image_url('heart-icon.png') %>"
      window.profileURL = "<%= image_url('profile-icon.png') %>"
      window.locationURL = "<%= image_url('location-icon.png') %>"
      window.defaultProfilePicURL = "<%= image_url('default_profile_pic.png') %>"
    </script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
